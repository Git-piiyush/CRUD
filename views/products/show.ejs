<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= product.name %>
    </title>
</head>

<body>
    <h1>
        <%= product.name %>
    </h1>
    <ol>
        <li>Price: <%= product.price %>
        </li>
        <li>Category: <a href='/shop?category=<%= product.category %>'>
                <%= product.category %>
            </a>
        </li>
    </ol>

    <a href='/shop'>Show all</a>
    <a href='/shop/<%= product._id %>/edit '>Edit the item</a>
    <div>
        <form action="/products/<%= product._id %>?_method=DELETE " method="POST">
            <button>Delete</button>
        </form>
    </div>
</body>

</html>